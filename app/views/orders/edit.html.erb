<h2>Editing Product</h2>
<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this product from being saved:</h2>
      <ul>
      <% @orders.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <div class="field">
    <%= f.label :status %><br>
    <%= f.select(:status, ['new','i guess someday','done'], )%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  <% end %>


